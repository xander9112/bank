import 'package:flutter/widgets.dart';

class UiSelectRole extends StatelessWidget {
  const UiSelectRole({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
